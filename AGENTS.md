# AGENTS.md

## 1. 文档定位

本文件用于指导 Codex 在当前**新项目**中进行分析、实现、重构、修复、文档补全、工程整理与长期维护。

本项目是一个新的独立工程，不继承旧项目的具体目录、包名、启动脚本和历史修补逻辑。虽然项目通用工程标准与之前保持一致，但必须把它视为**新仓库、新主线、新设计基准**来执行。

建议的总体技术环境：

- Windows + WSL2
- **WSL 发行版名称：`Ubuntu`**
- 发行版内部系统版本：**Ubuntu 24.04**
- VS Code Remote - WSL
- ROS 2 Jazzy
- Gazebo Harmonic
- RViz2
- C++17 及以上
- CMake + colcon

项目总体目标：

- 机械臂仿真支持**独立运行**
- 后续可扩展到末端工具、传感器、目标球、复杂环境与多模块联动
- 机械臂主模型以后续项目内确定的 URDF 为基准
- 控制系统采用**分层架构**
- 必须具备**轨迹规划层**
- 必须具备**LQR 跟踪控制层**
- 必须具备**FOC 电机控制层**
- 必须具备**CAN 虚拟通信层**
- 必须具备**Gazebo 物理执行层**
- 文档、注释、说明统一采用**简体中文**
- 注释风格采用 **Google 风格**
- 工程必须保持**模块化、规范化、高复用、高内聚、低耦合**
- 项目版本控制必须采用 **Git 严格管理**
- **每次任务结束必须同步更新文档**

---

## 2. 运行环境约束

### 2.1 WSL 发行版唯一性

当前 Windows 宿主机可能同时安装多个 WSL 发行版，但本项目唯一允许使用的发行版是：

- `Ubuntu`

Codex 必须遵守以下规则：

1. 所有构建、运行、调试、日志检查、依赖安装、路径引用，均以 **WSL 的 `Ubuntu` 发行版** 为准。
2. **禁止**混用其他发行版中的：
   - 工作区
   - 构建缓存
   - 环境变量
   - 安装依赖
   - 日志产物
3. 若检测到当前 shell 不在 `Ubuntu` 发行版中，必须停止继续执行并提醒切换到正确发行版。
4. 所有文档中的环境说明必须明确写出：
   - 项目运行在 **WSL 的 `Ubuntu` 发行版**
   - 内部系统版本为 **Ubuntu 24.04**
   - **不是**其他 WSL 发行版

### 2.2 默认执行基准

Codex 默认按以下基准执行：

1. 所有命令示例默认从**项目根目录**执行。
2. 所有路径优先使用**项目内相对路径**。
3. 所有构建和调试默认基于 **WSL shell**。
4. 默认 Python 使用系统 Python，而不是 Conda 环境中的 Python。
5. 默认 Gazebo / ROS / RViz 启动链路必须与项目内配置保持一致，不依赖不可追踪的系统外部资源路径。

---

## 3. 最高优先级原则

### 3.1 新项目独立性优先

本项目是**新开的项目**。Codex 必须遵守：

1. **禁止**把旧项目中的目录结构、包名、历史修补逻辑、旧 launch 链路直接机械复制到新项目。
2. 旧项目经验只可作为**设计参考**，不能作为未经审查的实现真源。
3. 若引用旧项目中的思路，必须先判断：
   - 是否仍适用于本项目
   - 是否会引入历史包袱
   - 是否与本项目当前基准 URDF、控制链、通信链一致
4. 新项目应优先建立**清晰、干净、单一主线**，避免“先复制旧修补层再继续叠加”的做法。

### 3.2 模型保真与单一基准

本项目机械臂描述必须坚持：

1. **项目内当前确定的主 URDF** 作为机械臂仿真基准。
2. 所有后续规划、控制、抓取、显示、碰撞分析都应优先围绕该基准 URDF 展开。
3. **禁止**同时长期维护多套相互冲突的机械臂主模型。
4. 若存在：
   - 原始导出 URDF
   - 手工修正 URDF
   - 运行导出 URDF
   则必须明确哪一个是**当前主基准**，并在文档中写清楚。
5. **禁止**把自动导出、临时调试、简化替代模型继续默认为长期主模型。

### 3.3 生成产物不可反客为主

Codex 必须区分以下内容：

#### A. 主基准描述层
- 当前项目确认使用的 URDF / xacro / mesh / 配置

#### B. 适配层
- 为 ROS 2 Jazzy / Gazebo Harmonic / ros2_control / MoveIt / Drake 做的兼容层

#### C. 生成产物
- `generated/...`
- `build/...`
- `install/...`
- `log/...`

规则：

1. `generated/` 只允许存放生成结果、导出结果、调试中间文件。
2. `build/`、`install/`、`log/` 均视为构建产物，不允许作为源码真源。
3. **禁止**把生成产物反过来当作长期唯一真源。

---

## 4. 系统控制架构原则

本项目采用**时间尺度分离**与**层级解耦**的系统设计。

### 4.1 控制层级划分

#### L1：全局规划层
职责：
- 目标点可达性检查
- 碰撞检查
- 约束处理
- 路径生成
- 关节轨迹生成
- 失败原因诊断

典型实现：
- Direct Collocation
- 轨迹优化
- 时间参数化

#### L2：跟踪控制层
职责：
- 对参考轨迹进行高频状态反馈
- 抑制扰动
- 生成修正控制量

典型实现：
- LQR
- 前馈 + 反馈组合控制

#### L3：虚拟通信层
职责：
- 承载控制报文
- 模拟总线延迟
- 模拟量化误差
- 模拟封包/解包

典型实现：
- ROS 2 + SocketCAN
- `vcan` 虚拟总线

#### L4：伺服驱动层
职责：
- 电流/扭矩闭环
- Clarke / Park 变换
- 电压限幅
- PWM 或平均电压模型
- 生成电机驱动输入

典型实现：
- FOC
- 平均电压模型
- 可选 SVPWM

#### L5：物理执行层
职责：
- 刚体运动学与动力学执行
- 接触与碰撞
- 状态反馈

典型实现：
- Gazebo Harmonic 作为机械物理真源

### 4.2 职责边界强约束

Codex 必须遵守：

1. **禁止**把 LQR 当成全局轨迹规划器使用。
2. **禁止**把“目标点 -> IK -> LQR/FOC 跟踪”视为完整运动方案。
3. **禁止**让 Gazebo 同时承担“电机电气方程解算”与“机械执行层”全部细节，而忽略独立电机仿真节点。
4. **禁止**让 Drake 与 Gazebo 同时各自作为完整机械物理真源。
5. 规划层输出必须作为控制层输入。
6. 控制层只能做跟踪和调节，不能替代规划。
7. 执行层只能做物理执行与反馈，不能替代控制器设计。
8. 文档中必须明确说明：
   - 规划层输入/输出
   - 控制层输入/输出
   - 通信层输入/输出
   - 执行层输入/输出
   - 各层之间的连接关系

---

## 5. FOC / LQR 自研约束（最高优先级之一）

### 5.1 核心原则

**本项目中，FOC 与 LQR 的核心控制逻辑和核心控制思想不允许采用已有控制库直接替代。**

这意味着：

1. **禁止**直接接入现成 FOC 控制库，把其作为项目核心实现。
2. **禁止**直接接入现成 LQR 控制库，把其作为项目核心实现。
3. **禁止**通过封装第三方控制库的方式伪装成“自研”。

### 5.2 允许与禁止的边界

#### 允许
可以使用以下基础设施类依赖：

- Eigen（矩阵运算）
- C++ 标准库
- ROS 2 通信框架
- Gazebo 接口
- 通用数值求解器基础设施
- 通用线性代数库
- 通用优化求解器（用于轨迹规划层，而非直接替代 LQR/FOC 核心控制逻辑）

#### 禁止
不允许把以下内容直接交给现成控制库完成：

- FOC 核心控制流程
  - Clarke 变换
  - Park 变换
  - dq 电流环
  - 扭矩/电流指令生成
  - 解耦逻辑
  - 电压限幅核心逻辑
- LQR 核心控制流程
  - 状态空间建模的核心控制主线
  - 代价函数主设计
  - 反馈律主线
  - 增益矩阵使用主链路
  - 核心控制决策逻辑

### 5.3 推荐实现要求

Codex 必须把以下内容视为**项目内核心源码资产**：

#### FOC 核心
- Clarke 变换
- Park 变换
- 逆 Park 变换
- dq 轴 PI / 控制律
- 电流目标生成
- 力矩目标到电流目标的映射
- 电压限幅
- 平均电压模型或 SVPWM 生成逻辑

#### LQR 核心
- 状态定义
- 线性化结果接入
- Q/R 权重组织
- 增益矩阵使用方式
- 参考状态构造
- 误差状态构造
- 控制输出组合逻辑

---

## 6. 电机仿真原则

### 6.1 电机仿真必须采用真实电气方程与力矩方程

本项目中，电机仿真不能退化为“简单一阶力矩块”或“理想力矩源”。

必须显式建模以下物理内容：

1. 电气方程
2. 反电动势
3. 电磁转矩生成
4. 电流状态更新
5. 电压限幅
6. 阻尼与摩擦
7. 与机械负载的耦合

### 6.2 建议的电机建模方式

优先采用 PMSM / BLDC 的 dq 模型或与当前电机参数一致的等效模型。

必须在代码与文档中明确：

- 极对数
- 线间电阻/电感
- 反电动势常数
- 力矩常数
- 母线电压
- 最大电流
- 最大电压
- 积分步长
- 使用平均电压模型还是开关级模型

### 6.3 线间值使用约束

本项目已明确：

- `phase_resistance`
- `phase_inductance`

配置中填写的是**线间值**，不是单相值。

因此 Codex 必须遵守：

1. 代码中若电机方程使用单相等效值，必须在实现中**显式做一次转换**。
2. **禁止**依赖口头约定或人工记忆去判断线间值/单相值。
3. 文档中必须明确说明：
   - 配置文件填的是线间值
   - 方程内部如何转换
   - 转换只做一次

### 6.4 避免机械惯量重复计入

若项目已经在 URDF 中手工修正了关节对应 link 的惯量（例如 `izz`），并且电机与关节臂在机械层被视为整体，则：

1. **禁止**在电机仿真层再次把同一部分机械惯量重复叠加到关节侧。
2. 必须在设计文档中明确：
   - 哪部分惯量由 URDF 承担
   - 哪部分惯量由电机模型承担
   - 是否保留转子内部附加惯量
3. 任何时候都要避免“URDF 惯量 + 电机机械惯量”双重计入导致的失真。

---

## 7. Drake 与 Gazebo 协同原则

### 7.1 可以一起使用，但必须分工

本项目允许同时使用 Drake 与 Gazebo，但必须遵守以下分工原则：

#### Drake 适合做
- Direct Collocation
- 轨迹优化
- 线性化
- LQR 设计
- 离线或低频参考轨迹生成

#### Gazebo 适合做
- 刚体动力学执行
- 场景仿真
- 碰撞与接触
- 关节状态反馈
- 可视化主场景

### 7.2 禁止事项

1. **禁止**让 Drake 与 Gazebo 同时都作为完整机械物理真源。
2. **禁止**让两者同时各自产生一套冲突的 joint state / TF / `/clock`。
3. **禁止**在未明确主从关系时并行叠加两套物理执行链。

### 7.3 推荐主从关系

推荐采用：

- **Drake = 规划与控制设计工具链**
- **Gazebo = 机械执行与碰撞真源**

---

## 8. 仿真提速原则（不降低仿真难度）

Codex 若需提升仿真速度，必须优先采用以下方式，而**不是**通过粗暴简化物理本质来提速：

1. **多速率仿真**
   - 电机电气层高频
   - 机械执行层中频
   - 可视化低频

2. **平均电压模型优先**
   - 默认使用平均电压模型
   - 只有研究逆变器开关细节时才开启开关级 SVPWM

3. **刚性方程用合适求解器**
   - 电机电气方程优先使用适合刚性系统的方法

4. **缓存不随时间变化的量**
   - 参数转换
   - 固定变换矩阵
   - 样条系数
   - 报文解析表

5. **分离 visual mesh 与 collision mesh**
   - 保持视觉高保真
   - 碰撞几何采用几何等价但更轻量的模型

6. **降低日志与可视化输出频率，而不是降低物理建模精度**

---

## 9. 推荐项目结构

```text
<project-root>/
├── AGENTS.md
├── SKILLS.md
├── README.md
├── docs/
│   ├── architecture/
│   ├── control/
│   ├── planning/
│   ├── communication/
│   ├── simulation/
│   └── development/
├── config/
│   ├── global/
│   ├── arm/
│   ├── motor/
│   ├── foc/
│   ├── lqr/
│   ├── can/
│   ├── gazebo/
│   └── ros/
├── generated/
│   ├── urdf/
│   ├── logs/
│   └── temporary/
├── src/
│   ├── description/
│   ├── planning/
│   │   ├── direct_collocation_planner/
│   │   ├── trajectory_post_processor/
│   │   └── reachability_checker/
│   ├── control/
│   │   ├── lqr_controller/
│   │   ├── foc_controller/
│   │   ├── motor_physical_simulator/
│   │   └── actuation_bridge/
│   ├── communication/
│   │   ├── can_protocol/
│   │   ├── can_transport/
│   │   └── can_codec/
│   ├── simulation/
│   │   ├── gazebo_system/
│   │   ├── rviz_tools/
│   │   └── scenario_manager/
│   ├── common/
│   │   ├── math_utils/
│   │   ├── model_utils/
│   │   ├── logging_utils/
│   │   └── parameter_utils/
│   └── tools/
└── .vscode/
```

---

## 10. 文档要求

### 10.1 必须维护的文档

至少维护以下文档：

- `docs/architecture/system_overview.md`
- `docs/architecture/control_hierarchy.md`
- `docs/control/foc_design.md`
- `docs/control/lqr_design.md`
- `docs/control/motor_physics.md`
- `docs/planning/trajectory_planning_design.md`
- `docs/planning/reachability_design.md`
- `docs/communication/can_protocol.md`
- `docs/simulation/gazebo_execution_design.md`
- `docs/simulation/drake_integration_design.md`
- `docs/development/build_and_run.md`
- `docs/development/debug_guide.md`

所有文档必须使用简体中文，要求：

- 表达专业
- 结构清晰
- 结论明确
- 避免空泛描述

### 10.2 文档同步更新要求

**每次任务结束必须同步更新文档。**

Codex 必须遵守：

1. 任何涉及以下内容的修改，都必须同步更新对应文档：
   - 模型结构
   - 坐标系定义
   - 规划算法
   - LQR 核心设计
   - FOC 核心设计
   - 电机参数
   - 电机方程
   - 通信协议
   - 启动方式
   - 调试方法
   - 已知限制
2. **禁止**只改代码不改文档。
3. 若任务中产生新的：
   - 配置参数
   - 接口字段
   - 失败原因诊断逻辑
   - 日志格式
   必须写入文档。

---

## 11. 代码与架构要求

### 11.1 C++ 实现要求

- 主要实现语言必须为 C++
- 使用现代 C++（推荐 C++17 或以上）
- 模块边界清晰
- 禁止把规划、控制、通信、仿真适配逻辑耦合到同一源文件

### 11.2 架构要求

必须满足：

- 高内聚
- 低耦合
- 高复用
- 明确边界
- 规划层、控制层、通信层、仿真层、电机物理层解耦

### 11.3 输出要求

终端输出优先保持**结构化、对齐、稳定格式**，并使用**简体中文**。

默认输出应尽量包含：

- 当前规划状态
- 当前目标点位置
- 当前末端位置
- 关节目标角度
- 关键控制状态
- 解析后的 CAN 报文内容

---

## 12. Git 规则

### 12.1 版本控制原则

- 所有结构性修改必须通过 Git 提交
- 禁止把 `build/`、`install/`、`log/` 纳入版本控制
- 禁止把临时修复逻辑直接长期保留而不文档化

### 12.2 提交建议

推荐前缀：

- `feat:`
- `fix:`
- `refactor:`
- `docs:`
- `chore:`

---

## 13. 禁止事项

Codex 严禁执行以下操作：

1. 把旧项目的历史补丁链未经审查直接复制进新项目。
2. 把简化模型长期当成新项目主模型。
3. 把 `generated/*.urdf` 当成长期唯一真源。
4. 直接接入现成 FOC 核心库作为项目核心实现。
5. 直接接入现成 LQR 核心库作为项目核心实现。
6. 将 LQR、FOC 或简单 IK 跟踪误当成完整轨迹规划解决方案。
7. 让 Drake 与 Gazebo 同时作为完整机械物理真源。
8. 在未明确线间值/单相值定义时直接写死电机方程参数。
9. 在 URDF 已计入机械惯量后，再把同一机械惯量重复加进电机机械方程。
10. 只改代码不改文档。

---

## 14. Codex 执行建议

当 Codex 接到任务时，推荐按以下顺序执行：

1. 先确认当前任务属于：
   - 模型层
   - 规划层
   - 控制层
   - 电机仿真层
   - 通信层
   - 仿真层
   - 文档层
2. 若涉及控制问题，先明确问题位于：
   - L1 规划层
   - L2 跟踪层
   - L3 通信层
   - L4 FOC 层
   - L5 机械执行层
3. 若涉及电机参数，先确认：
   - 使用线间值还是单相值
   - 是否已在代码中做转换
   - 是否存在惯量重复计入
4. 每次新增模块、目录、接口、配置、文档时，保持与本 AGENTS 结构一致。
5. 每次任务结束，必须同步更新对应文档。
